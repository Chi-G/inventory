<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /inventory/

    # Handle everything by passing it to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]
</IfModule>