<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /inventory/

    # Handle the root request
    RewriteRule ^$ public/index.php [L]

    # Handle everything else (like /login)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/index.php [L]
</IfModule>